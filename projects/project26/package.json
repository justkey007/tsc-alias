{
  "name": "project22",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig/tsconfig.json && tsc-alias --project tsconfig/tsconfig.json",
    "start": "npm run build && node ./dist/index.js"
  }
}
